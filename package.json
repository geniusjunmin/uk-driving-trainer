{
  "name": "uk-driving-trainer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run --passWithNoTests",
    "smoke:browser": "node scripts/browser-smoke.mjs",
    "verify:deploy": "npm run test && npm run build && npm run smoke:browser"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.17.3",
    "three": "^0.179.1"
  },
  "devDependencies": {
    "@types/three": "^0.179.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
